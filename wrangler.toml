# DEPRECATED: This file is kept for reference only.
# banproof-me has been consolidated into the goldshore-ai monorepo.
#
# The authoritative wrangler.toml is now at:
#   /goldshore-ai/apps/banproof-me/wrangler.toml
#
# All deployments for banproof-me are now handled by the goldshore-ai
# monorepo deployment workflow (deploy-platform.yml).
#
# This satellite repository remains archived for reference but should
# NOT be deployed independently. See deploy.yml for deployment status.
#
# Keep this root manifest intentionally inert: do not add routes or
# queue consumers here. gateway/wrangler.toml owns the deployable
# banproof.me Worker routes (including preview.banproof.me/*) and the
# goldshore-jobs queue consumer; apex routes remain forbidden by
# ops/cloudflare-required.json so the Pages-hosted public site is not
# moved behind this Worker.
