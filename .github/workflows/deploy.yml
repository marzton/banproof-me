name: Deploy Worker (DISABLED - Consolidated to Monorepo)

# DEPRECATED: banproof-me deployments are now handled by the goldshore-ai monorepo.
#
# This workflow is kept for reference only and will not execute.
# See: https://github.com/marzton/goldshore-ai/blob/main/.github/workflows/deploy-platform.yml
#
# The banproof-me worker source has been consolidated into:
#   goldshore-ai/apps/banproof-me/
#
# All deployments now happen automatically when the monorepo is updated.
# To deploy banproof-me changes:
#   1. Push code to goldshore-ai/apps/banproof-me/
#   2. Trigger: goldshore-ai/.github/workflows/deploy-platform.yml
#   3. Satellite repo (marzton/banproof-me) is archived for reference only

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. All banproof-me deployments are managed by goldshore-ai monorepo."
